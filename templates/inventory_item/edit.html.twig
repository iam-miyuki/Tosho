{% extends 'base.html.twig' %}

{% block title %}Modifier l’entrée inventaire{% endblock %}

{% block body %}
	<div class="page-title">
		<h3>Modifier une entrée d’inventaire</h3>
		<img src="{{ asset('img/books.svg') }}" alt="inventory">
	</div>


	{{ form_start(itemForm) }}
		{{ form_row(itemForm) }}
		<button type="submit">Enregistrer les modifications</button>
	{{ form_end(itemForm) }}
{% endblock %}
