<div class="card one-card success-card">
	<div class="card-title">
		{{successMessage}}
	</div>
	<div class="icon-success"></div>
	<div class="btn-wrapper">
		<div class="href-btn">
			<a href="{{path('show-inventory',{tab:'search',id:addedInventory.id})}}">Voir</a>
		</div>
		<div class="href-btn">
			<a href="{{path('inventory',{tab:'new'})}}">Programmer un autre inventaire</a>
		</div>
		<div class="href-btn">
			<a href="{{path('inventory',{tab:'search'})}}">Retour</a>
		</div>
	</div>
	
</div>
