{% extends 'base.html.twig' %}

{% block body %}
	<div class="page-title">
		<h3>Prêts & Retours</h3>
		<img src="{{ asset('img/child-book.svg') }}" alt="book">
	</div>

	<div class="tab-container">
		<div class="tabs">
			<a href="{{ path('loan', {tab: 'family'}) }}" class="tab left-tab family-tab {{ tab == 'family' ? 'active' : '' }}"><div class="tab-icon"></div>Chercher par famille</a>
			<a href="{{ path('loan', {tab: 'book'}) }}" class="tab right-tab book-tab {{ tab == 'book' ? 'active' : '' }}"><div class="tab-icon"></div>Chercher par livre</a>
		</div>

		<div class="tab-content left-tab family-tab {{ tab == 'family' ? 'active' : '' }}">
			{% include 'loan/_search_family_tab.html.twig' %}
		</div>

		<div class="tab-content right-tab book-tab {{ tab == 'book' ? 'active' : '' }}">
			{% include 'loan/_search_book_tab.html.twig' %}
		</div>
	</div>
{% endblock %}



