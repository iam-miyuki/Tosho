{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}

<div class="page-title">
		<h3>Mon compte </h3>
		<img src="{{ asset('img/profile-3.svg') }}" alt="book">
	</div>
    
{{ form_start(form) }}
		{{ form_row(form) }}
		<button type="submit">Modifier</button>
	{{ form_end(form) }}
{% endblock %}
